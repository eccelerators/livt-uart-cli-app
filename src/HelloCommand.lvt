namespace Livt.App

using Livt.IO

component HelloCommand: CommandInterface
{
	uart: Uart

	new(uart: Uart)
	{
		this.uart = uart
	}

	override fn Execute()
	{
		var message = "Hello".Encode()
		this.uart.Send(message)
	}
}
