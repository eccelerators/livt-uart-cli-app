namespace Livt.App

using Livt.IO

component HelpCommand: CommandInterface
{
	uart: Uart

	new(uart: Uart)
	{
		this.uart = uart
	}

	override fn Execute()
	{
		var message = "hello bye".Encode()
		this.uart.Send(message)
	}
}
