namespace Livt.App

using Livt.IO

component UnknownCommand: CommandInterface
{
	uart: Uart

	new(uart: Uart)
	{
		this.uart = uart
	}

	override fn Execute()
	{
		var message = "Unknown".Encode()
		this.uart.Send(message)
	}
}
